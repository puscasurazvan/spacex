import { createGlobalStyle } from 'styled-components';

export const GlobalStyle = createGlobalStyle`
  @font-face {
    font-family: 'Orbitron Regular';
    src: url('/fonts/Orbitron_Regular.woff') format('woff');
    font-display: swap;
  }
`;
